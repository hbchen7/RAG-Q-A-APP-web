# 知识库界面技术方案

## 1. 目标

开发一个用户友好的知识库管理界面，允许用户创建、浏览知识库，并向其中上传和管理文件，为后续的 RAG (Retrieval-Augmented Generation) 功能提供数据基础。

## 2. 技术栈

- **前端框架**: Vue 3 (Composition API)
- **包管理**: pnpm
- **UI 库**: Element Plus
- **图标库**: @element-plus/icons-vue
- **HTTP 请求**: axios
- **状态管理**: Pinia
- **打包工具**: Vite
- **CSS 预处理器**: Sass
- **代码规范**: ESLint 9 + Prettier

## 3. 核心功能

- **知识库列表展示**:
  - 获取并显示所有知识库的基本信息（标题、创建者等）。
  - 允许用户选择一个知识库以查看详情。
  - 默认选中第一个知识库。
- **创建知识库**:
  - 通过弹窗收集新知识库信息（标题、标签、描述、嵌入配置）。
  - 嵌入配置包括嵌入模型名称和 API Key。
  - 调用后端 API 创建。
- **知识库详情展示**:
  - 显示选中知识库的详细信息（标题、创建人、创建时间、标签、描述）。
- **文件上传**:
  - 提供文件上传区域（支持拖拽）。
  - 将文件上传至当前选中的知识库。
  - 调用后端 API 上传。
- **文件列表展示**:
  - 显示选中知识库包含的文件列表（文件名、上传时间）。
- **文件删除**:
  - 提供删除文件的操作按钮。
  - 调用后端 API 删除，并提供二次确认。

## 4. 页面结构与组件拆分

- **`KnowledgeBase.vue` (主视图)**:
  - 整体布局：采用左右分栏 (`ElContainer`, `ElAside`, `ElMain`)。
  - 负责集成侧边栏和详情区域。
  - 协调状态管理和子组件交互。
- **`KnowledgeSidebar.vue` (侧边栏组件 - 可选)**:
  - 放置在 `ElAside` 中。
  - 包含 "新建知识库" 按钮 (`ElButton`)。
  - 展示知识库列表 (`ElMenu` 或自定义列表)，处理知识库选择事件。
- **`KnowledgeDetail.vue` (详情区域组件 - 可选)**:
  - 放置在 `ElMain` 中。
  - 包含知识库信息展示区 (`ElDescriptions`, `ElTag`)。
  - 包含 `FileUploadArea.vue` 组件。
  - 包含 `FileList.vue` 组件。
- **`CreateKnowledgeBaseDialog.vue` (创建弹窗组件)**:
  - 基于 `BaseFormDialog.vue` 或 `CreateAssistantDialog.vue` 结构。
  - 包含创建知识库所需的表单 (`ElForm`, `ElInput`, `ElSelect` 等)。
  - 处理表单校验和创建逻辑。
- **`FileUploadArea.vue` (文件上传组件 - 可选)**:
  - 使用 `ElUpload` 组件，配置拖拽上传。
  - 处理文件选择和自定义上传请求 (`:http-request`)。
- **`FileList.vue` (文件列表组件 - 可选)**:
  - 使用 `ElTable` 或 `ElList` 展示文件信息。
  - 包含文件操作按钮（如删除，使用 `ElDropdown`）。
- **`BaseFormDialog.vue` (复用)**: 用于删除文件的二次确认。

_初期开发可将逻辑集中在 `KnowledgeBase.vue`，视复杂度再行拆分。_

## 5. 状态管理 (Pinia)

创建一个新的 Store 模块： `src/stores/modules/knowledge.js`

- **`State`**:
  - `knowledgeBases: []`: 知识库列表。
  - `selectedKbId: null`: 当前选中的知识库 ID。
  - `loading: false`: 列表或操作的加载状态。
  - `error: null`: API 请求错误信息。
- **`Actions`**:
  - `fetchKnowledgeBases()`: 调用 `getKnowledgeList` API 获取列表，更新 `knowledgeBases` 和 `localStorage`，设置 `selectedKbId` (默认为第一个)。
  - `setSelectedKbId(id)`: 更新 `selectedKbId`。
  - `createKnowledgeBase(data)`: 调用 `createKnowledge` API，成功后调用 `fetchKnowledgeBases()` 刷新。
  - `uploadFile(kbId, formData)`: 调用 `uploadFileToKnowledgeBase` API，成功后调用 `fetchKnowledgeBases()` 刷新 (或局部更新 `knowledgeBases` 中的 `filesList`)。
  - `deleteFile(kbId, fileMd5)`: 调用 `deleteFileFromKnowledgeBase` API，成功后调用 `fetchKnowledgeBases()` 刷新 (或局部更新 `knowledgeBases` 中的 `filesList`)。
  - `loadFromLocalStorage()`: 应用初始化时尝试从 `localStorage` 加载缓存数据。
- **`Getters`**:
  - `selectedKnowledgeBase()`: 根据 `selectedKbId` 计算并返回完整的选中知识库对象。
- **`Persistence`**: 配置 Pinia Persist 插件，将 `knowledgeBases` 和 `selectedKbId` 持久化到 `localStorage`。

## 6. 主要流程

1.  **页面加载**:
    - `KnowledgeBase.vue` 挂载时，调用 `knowledgeStore.loadFromLocalStorage()` 尝试加载缓存。
    - 调用 `knowledgeStore.fetchKnowledgeBases()` 从 API 获取最新列表。
    - 列表加载成功后，默认选中第一个知识库，触发详情区域的渲染。
2.  **创建知识库**:
    - 点击 "新建知识库" 按钮，打开 `CreateKnowledgeBaseDialog.vue`。
    - 用户填写表单，确认后触发表单校验。
    - 校验通过，调用 `knowledgeStore.createKnowledgeBase(formData)`。
    - 创建成功后，Store 会刷新列表，UI 自动更新。
3.  **选择知识库**:
    - 用户在侧边栏点击不同的知识库。
    - 触发 `knowledgeStore.setSelectedKbId(newId)`。
    - `KnowledgeDetail.vue` (或 `KnowledgeBase.vue`) 监听到 `selectedKnowledgeBase` getter 的变化，重新渲染详情、文件上传区和文件列表。
4.  **上传文件**:
    - 用户通过 `FileUploadArea.vue` 选择或拖拽文件。
    - 组件触发 `knowledgeStore.uploadFile(selectedKbId, formData)`。
    - 上传成功后，Store 刷新列表 (或更新文件列表)，UI 更新。
5.  **删除文件**:
    - 用户点击文件列表中的删除按钮。
    - 弹出 `BaseFormDialog.vue` 进行二次确认。
    - 用户确认后，调用 `knowledgeStore.deleteFile(selectedKbId, fileMd5)`。
    - 删除成功后，Store 刷新列表 (或更新文件列表)，UI 更新。

## 7. 关键组件设计细节

- **`CreateKnowledgeBaseDialog.vue`**:
  - **表单字段**: 标题 (Input), 标签 (Input/TagInput - 可选), 描述 (Textarea - 可选), 嵌入模型 (Input/Select - **来源待定**), API Key (Input - 可预填)。
  - **校验**: 标题、模型、API Key 为必填项。
- **`FileUploadArea.vue`**:
  - `ElUpload`: 设置 `drag`, `action` (设为空或任意值，因使用 `:http-request`), `:http-request` 指向自定义上传函数。
  - 自定义上传函数: 构造 `FormData`，调用 `knowledgeStore.uploadFile`。处理上传进度和反馈。
- **`FileList.vue`**:
  - `ElTable`: 列包含文件名、上传时间、操作。
  - 操作列: 使用 `ElDropdown` 包含 "删除" 选项。

## 8. API 交互

- `getKnowledgeList` (GET `/knowledge/`)
- `createKnowledge` (POST `/knowledge/`)
- `uploadFileToKnowledgeBase` (POST `/knowledge/{kbId}/files/`)
- `deleteFileFromKnowledgeBase` (DELETE `/knowledge/{kbId}/files/{fileMd5}`)
- `deleteKnowledgeBase` (DELETE `/knowledge/{kbId}`) - _（将用于删除整个知识库）_

## 9. 持久化策略

- 使用 `pinia-plugin-persistedstate`。
- 将 `knowledgeStore` 中的 `knowledgeBases` 和 `selectedKbId` 存储在 `localStorage` 中。
- 应用加载时优先从 `localStorage` 读取，然后异步请求 API 更新。
- 所有增删改操作成功后，同步更新 Pinia State 和 `localStorage`。

## 10. 待确认 / 后续优化

- **嵌入模型列表来源**: 需要明确创建知识库时，"嵌入模型" 的可选列表从何而来？是固定值、来自 OneAPI 模型列表、单独的 API 接口，还是用户手动输入？
- **API Key 格式**: 确认 `embedding_apikey` 字段需要的是完整 `sk-...` 还是仅令牌值。
- **知识库删除功能**: 是否需要在界面上提供删除整个知识库的功能？
- **错误处理**: 完善所有 API 调用的错误处理和用户反馈。
- **加载状态**: 在列表加载、文件上传/删除等耗时操作期间，添加明确的 Loading 状态。
- **性能优化**: 如果知识库或文件列表变得非常大，考虑使用分页加载或虚拟滚动优化性能，并将 refetch 改为局部更新 state。
- **UI 细节**: 细化各组件的样式和交互细节。
